To Run:
1. Place your CSV and JSON files into the json_replacer folder
2. Change values in settings.py to match filenames and target values
3. In bash run python /path/to/json_replacer/json_replacer.py
